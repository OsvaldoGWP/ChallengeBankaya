# SERVER CONFIG
server:
    port: 8080

logging:
    level:
        ROOT: INFO
        web: DEBUG
        org.springframework.ws.server.MessageTracing.received: TRACE
        com.bankaya: DEBUG

feign:
    client.config:
        default:
            logger-level: FULL

api:
    pokemon: https://pokeapi.co/api/v2